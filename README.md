# ai_provider_kimi_coding_plan

这是 FamilyClaw 的 Kimi Coding Plan AI Provider 插件。

当前仓库同步的是插件目录本身，包含：

- `driver.py`：插件驱动入口
- `manifest.json`：插件清单与配置描述
- `resources/`：插件展示资源

这个仓库只维护插件代码，不包含 FamilyClaw 主仓库里的其他模块。
